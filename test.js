const timeOfDay = 'morning'

const greet = (name) => {
    return `How are you doing, ${who}!`
}

//can export functions, const, let, var, etc.
module.exports = {
    timeOfDay,
    greet
}

console.log(module)